import { createRoot } from "react-dom/client";
import App from "./App";

createRoot(document.getElementById("root")!).render(<App />);

//import { ErrorExamples } from './ErrorExamples';
//createRoot(document.getElementById("root")!).render(<ErrorExamples />);
